version: 2023-03
namespace: demo-ghost

environment: dev

quotas:
  cpu: "1"
  memory: 1Gi

fargate: false

application:
  name: ghost
  container:
    image: ghost
    tag: latest

service:
  type: ClusterIP
  port: 3001
